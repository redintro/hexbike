/**
 * This package contains adapters that drive our application (because they call our application core). Adapters herein
 * are categorised as inbound as the data flows in from the adapter to the application core.
 */
package io.redintro.hexbike.adapter.in;
